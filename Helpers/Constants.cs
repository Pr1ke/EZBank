namespace EZBank.Helpers
{
    internal class Constants
    {
        //We should create a initializer class that prepares the SQL Server and creates a dynamic DB name but this is fine for now
        public static string dbName = "EZBank";  
    }
}
